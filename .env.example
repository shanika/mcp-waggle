# Path to the SQLite database file. Defaults to ~/.waggle/waggle.db
DB_PATH=

# Admin password you choose. Gates the OAuth consent page (HTTP transport)
# and the dashboard login (`waggle ui`). Required for both; never persisted.
OAUTH_ADMIN_PASSWORD=
